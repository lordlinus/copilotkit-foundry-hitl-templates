apiVersion: v1
metadata:
  name: forgewright
  displayName: forgewright Templates
  description: One-prompt CopilotKit + AG-UI + Foundry hosted-agent apps with HITL
entries:
  - path: templates/agentic-copilot-foundry
    name: Agentic CopilotKit + Foundry (HITL)
    description: A Next.js/CopilotKit chat UI over a FastAPI/AG-UI SSE backend hosting one Microsoft Agent Framework agent, connected keyless to Azure AI Foundry, with native human-in-the-loop approval on consequential tools. Also publishable as a Foundry hosted agent (Responses) via azd.
  - path: templates/conversational-banking
    name: Conversational Banking (HITL)
    description: A conversational banking assistant: check balances and recent activity freely, but every money movement (transfer, bill pay) pauses on an Approve/Reject widget before it executes. CopilotKit + AG-UI over one Microsoft Agent Framework agent, keyless to Azure AI Foundry; also publishable as a Foundry hosted agent via azd.
  - path: templates/health-claim-intake
    name: Health Insurance Claim Intake (HITL)
    description: Intake multiple claim documents, auto-fill the claim form, let the user review and edit, then submit to the insurer behind a human-in-the-loop approval gate. CopilotKit + AG-UI over one Microsoft Agent Framework agent, keyless to Azure AI Foundry; also publishable as a Foundry hosted agent via azd.
