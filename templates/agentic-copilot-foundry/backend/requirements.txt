# DEPLOYED bridge image deps. The bridge runs no model and no tools: it forwards
# AG-UI turns + mcp_approval_response to the Foundry HOSTED agent over Responses.
# It needs agent-framework-core (Content types) + ag-ui (the AG-UI endpoint) +
# httpx (the Responses driver; PINNED stable — prerelease pulls httpx 1.0.dev with
# no AsyncClient) + aiohttp (async transport for DefaultAzureCredential.aio).
agent-framework-core==1.9.0
agent-framework-ag-ui==1.0.0rc5
httpx==0.28.1
aiohttp>=3.10.0
azure-identity>=1.19.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
