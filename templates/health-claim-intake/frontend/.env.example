# Copy to frontend/.env.local (gitignored).
# URL of the AG-UI backend (FastAPI + SSE). Default works for local dev.
AG_UI_BACKEND_URL=http://localhost:8080/
# Set only if the backend enforces AG_UI_API_KEY.
# AG_UI_API_KEY=
