# Python
__pycache__/
*.pyc
backend/.venv/
src/.venv/
backend/.env
.env

# Node / Next
node_modules/
frontend/.next/
frontend/.env.local

# azd — `azd ai agent run` creates ./.azure at the app root; `make up` /
# `make up-app` create hosted/.azure and deploy/.azure. All hold env-specific
# endpoints/subscription ids and must never be committed.
.azure/
