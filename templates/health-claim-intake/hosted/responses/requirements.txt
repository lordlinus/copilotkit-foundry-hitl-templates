# agent-framework-core (NOT the agent-framework meta-package — it drags in
# agent-framework-hyperlight → hyperlight-sandbox-backend-wasm and fails Foundry's
# remote build). mcp is imported by agent_framework_foundry_hosting and is not
# pulled in transitively, so it must be explicit.
agent-framework-core==1.8.1
agent-framework-openai==1.8.1
agent-framework-foundry-hosting
mcp>=1.24.0,<2
azure-ai-documentintelligence>=1.0.0
azure-identity>=1.19.0
python-dotenv>=1.0.0
