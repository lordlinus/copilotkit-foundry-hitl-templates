# App targets. Include from Makefile (which sets APP_NAME / AGENT_NAME).
SHELL := /bin/bash
PY    ?= python3
UV    ?= uv
VENV  := backend/.venv
BPY   := $(VENV)/bin/python

.DEFAULT_GOAL := help
.PHONY: help doctor preflight install local verify smoke e2e up down deploy up-app deploy-app verify-deployed clean

help:
	@echo "$(APP_NAME) — the path, in order ('make doctor' checks your setup at any point):"
	@echo ""
	@echo "  one-time setup"
	@echo "    1. make verify             offline structural gate — green before you touch anything"
	@echo "    2. az login && azd auth login    azd keeps its own credential; BOTH are needed"
	@echo "    3. make up                 provision + deploy the hosted Foundry agent"
	@echo "                               (first run prompts: env name, subscription, location —"
	@echo "                               only some regions support hosted agents; 'make up' checks)"
	@echo ""
	@echo "  prove it (the dev gate — rerun after every change; NOT deployment proof)"
	@echo "    4. make smoke              end-to-end HITL vs the REAL agent run locally via"
	@echo "                               'azd ai agent run' (reuses the project 'make up' just"
	@echo "                               provisioned): read works, action PAUSES, approve executes,"
	@echo "                               reject doesn't"
	@echo "    5. make e2e                the same journey driven in a real Chromium browser"
	@echo "       make local              dev loop: agent :8088 + bridge :8080 + UI :3000"
	@echo ""
	@echo "  ship it (required before calling the app deployed / live)"
	@echo "    6. make up-app             deploy the bridge + frontend as Container Apps"
	@echo "    7. make verify-deployed    a REAL active Foundry agent answers a live invoke"
	@echo ""
	@echo "  utilities"
	@echo "    make doctor      check tools, logins, azd envs, ports — with the fix for each failure"
	@echo "    make preflight   install backend venv + frontend deps"
	@echo "    make deploy      redeploy the hosted agent after src/agent.py changes (skips provisioning)"
	@echo "    make deploy-app  redeploy the bridge + frontend after code changes (skips provisioning)"
	@echo "    make down        tear down ALL provisioned Azure resources (deploy/ then hosted/)"
	@echo "    make clean       remove venvs / node_modules / .next"

doctor:
	@bash scripts/doctor.sh

install:
	@command -v $(UV) >/dev/null || { echo "uv is required — install from https://docs.astral.sh/uv/"; exit 1; }
	@test -d $(VENV) || $(UV) venv --python 3.12 $(VENV)
	@$(UV) pip install -q --python $(BPY) --prerelease=allow -r backend/requirements.txt
	@test -d frontend/node_modules || (cd frontend && npm install)

preflight: install
	@command -v node >/dev/null || { echo "node is required"; exit 1; }
	@echo "✓ preflight ok"

verify:
	@bash scripts/verify.sh

up:
	@bash scripts/check-region.sh
	@cd hosted && azd up

smoke: install
	@bash scripts/smoke_run.sh

e2e: install
	@bash scripts/e2e_run.sh

local: install
	@bash run-local.sh

up-app:
	@cd deploy && azd up

verify-deployed:
	@bash scripts/verify-deployed.sh

deploy:
	@cd hosted && azd deploy

deploy-app:
	@cd deploy && azd deploy

down:
	@echo "▸ tearing down deployed infra (deploy/ first — the bridge/frontend depend on the"
	@echo "  hosted agent, not the other way round)…"
	@if [ -d deploy/.azure ]; then (cd deploy && azd down --purge --force); \
	 else echo "  (deploy/.azure not found — nothing to tear down there)"; fi
	@if [ -d hosted/.azure ]; then (cd hosted && azd down --purge --force); \
	 else echo "  (hosted/.azure not found — nothing to tear down there)"; fi
	@echo "✓ down — all provisioned Azure resources removed (local files/azd envs untouched)"

clean:
	@rm -rf $(VENV) frontend/node_modules frontend/.next backend/__pycache__ src/__pycache__
	@echo "✓ cleaned"
