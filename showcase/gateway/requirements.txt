# Showcase gateway dependencies — pins the LATEST agent-framework stack (the
# templates pin older versions for their own verified baseline; the showcase runs
# their code against current releases). Re-run `make smoke` after any bump.
#
# agent-framework-openai >= 1.1 routes `.../openai/v1` base URLs to AsyncOpenAI
# (no api-version), so the templates' keyless Foundry branch works natively — no
# shim required. Ref: microsoft/agent-framework _shared.py.
agent-framework==1.8.1
agent-framework-openai==1.8.1
agent-framework-ag-ui==1.0.0rc4

# Deps imported by the template backends (src/agent.py, backend/ag_ui_app.py).
azure-identity>=1.19.0
python-dotenv>=1.0.0

# Gateway runtime (FastAPI reverse-proxy + supervisor).
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
