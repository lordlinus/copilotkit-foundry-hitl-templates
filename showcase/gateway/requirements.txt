# Showcase gateway dependencies. The container runs, per MAF agent, BOTH the
# Foundry hosted-agent runtime (ResponsesHostServer) AND the thin AG-UI bridge —
# the same two-process stack the templates use locally (`make local`) and deploy.
# So this is the UNION of the templates' root requirements.txt (hosted runtime)
# and backend/requirements.txt (bridge), pinned to the templates' verified stack.
# Re-run `make smoke` after any bump.

# ── Hosted-agent runtime (ResponsesHostServer = <template>/app.py) ────────────
agent-framework-core==1.11.0
agent-framework-foundry==1.10.1
agent-framework-foundry-hosting==1.0.0a260709
mcp>=1.24.0,<2

# ── AG-UI bridge (bridge_app:app, DIRECT mode -> the runtime) ─────────────────
agent-framework-ag-ui==1.0.0rc8
httpx==0.28.1
aiohttp>=3.10.0

# ── Shared + gateway runtime (FastAPI reverse-proxy + supervisor) ─────────────
azure-identity>=1.19.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
