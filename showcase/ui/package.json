{
  "name": "forgewright-showcase-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Static gallery + AG-UI chat client for the forgewright showcase. Talks directly to the always-on gateway over AG-UI SSE — no CopilotKit runtime, deployable to GitHub Pages.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@ag-ui/client": "0.0.56"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
