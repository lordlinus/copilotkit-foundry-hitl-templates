// Runtime config for the showcase UI. The GitHub Pages workflow OVERWRITES this
// file at publish time, stamping window.SHOWCASE_API_BASE from the `API_BASE`
// repository variable (the always-on gateway's Container App URL).
//
// Locally, leave it empty: the UI falls back to http://localhost:8080.
window.SHOWCASE_API_BASE = "";
